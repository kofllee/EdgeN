package kofllee.edgen.graphic;

public class MainWindow extends Window {

    public MainWindow(String title, int width, int height, boolean vSync, boolean resizable){
        super(title, width, height, resizable, vSync);
    }
}
